import { render, screen } from "@testing-library/react";
import { Content } from "./content";

describe("tickerTracker", () => {
	// const server = setupServer(http.get('loadTickers', () => {
	//     return "tickers"
	// }));

	it("renders Content", () => {
		//render(<Content></Content>);

		screen.debug();
	});
});
